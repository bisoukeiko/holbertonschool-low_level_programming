/**
 * _isdigit - check for a digit(0 through 9)
 * @c: number to be checked
 * Return: 1 c is a digit
 *         0 otherwise
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{

		return (1);

	}
	else
	{

		return (0);

	}
}

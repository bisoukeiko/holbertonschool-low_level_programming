#include "main.h"
#include <stdlib.h>

/**
 * _abs - compute the absolute value of an integer
 * @num: integer
 * Return: the absolute value of an integer
 */

int _abs(int num)
{
	return (abs(num));
}

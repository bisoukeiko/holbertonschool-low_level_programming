#include "main.h"

/**
 * _strlen - return the length of a string
 * @s: pointer of string
 * Return: length of a string
 */

int _strlen(char *s)
{

	char str[] = *s;
	int cnt;

	for (cnt = 0; str[cnt] != '\0'; ++cnt)
	
	return cnt;

}

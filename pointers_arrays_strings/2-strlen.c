#include "main.h"

/**
 * _strlen - return the length of a string
 * @s: pointer of string
 * Return: length of a string
 */

int _strlen(char *s)
{

	int cnt;

	for (cnt = 0; s[cnt] != '\0'; ++cnt)
	{
		continue;
	}

	return cnt;

}
